from setuptools import find_packages, setup

setup(
    name="sensor",
    version="0.0.2",
    author="iNeuron",
    author_email="cloud@ineuron.ai",
    packages=find_packages(),
    install_requires=[],
)
